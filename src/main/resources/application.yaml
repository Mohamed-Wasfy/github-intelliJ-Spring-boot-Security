spring:
  application:
    name: spring-boot-security
  datasource:
    url: jdbc:mysql://localhost:3306/store_api?createDatabaseIfNotExist=true
    username: root
    password: admin
  jpa:
    show-sql: true
    open-in-view: false
    #Close DB Connection for Entire HTTP Request-Response and Open It only When Called and close it directly after service returns output.